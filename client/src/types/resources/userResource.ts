export interface IUserInfo {
    email: string;
    name: string;
    phone: string;
    isHaveAvatar: boolean;
    id: string;
}

export interface IFriendInfo {
    name: string;
    isHaveAvatar: boolean;
    id: string;
}
